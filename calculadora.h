#ifndef _calculadora
    #define _calculadora
    #include <stdio.h>
    #include <stdlib.h>

    long add(int, int);
    long subs(long, int);
    long multip(long, int);
    float division (long, int);


#endif
